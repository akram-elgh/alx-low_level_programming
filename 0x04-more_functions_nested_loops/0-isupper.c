#include "main.h"

/**
* isupper - checks if a character is uppercase
* @c: character to check
* 
* Return: 1 if c is upper, 0 otherwise
*/
int isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
