#include "main.h"
#include <stdlib.h>

/**
 * infinite_add - add 2 integers.
 * @n1: integer
 * @n2: integer
 * @r: buffer
 * @size_r: size of r
 * 
 * Return: char
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int a;
	int b;
	a = atoi(n1);
	b = atoi(n2);
}
